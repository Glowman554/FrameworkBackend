---
import AdministratorOnly from '../../components/AdministratorOnly';
import Layout from '../../components/Layout.astro';
import VersionList from '../../components/version/VersionList.astro';
import VersionNewButton from '../../components/version/VersionNewButton';

export const prerender = false;
---

<Layout title="Framework - Versions (internal)" description="For internal use only">
    <AdministratorOnly client:load />
    <VersionNewButton client:load />
    <VersionList editMode={true} />
</Layout>
